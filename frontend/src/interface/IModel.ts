export interface ModelData {
    model_id: string;     // รหัสประจำตัวของรุ่น
    model_name: string;   // ชื่อของรุ่น
    brand_id: string;     // รหัสประจำตัวของยี่ห้อที่รุ่นนี้เป็นของ
    type: string;         // ประเภทของรุ่น เช่น มือถือ, โน้ตบุ๊ค, PC, เมาส์, อื่นๆ
  }
  